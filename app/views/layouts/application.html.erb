<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <%= csrf_meta_tag %>
        
        <%= render 'layouts/stylesheets' %>
                
    </head>
    <body>
        <div class="container">
            <%= render 'layouts/header' %>
            <section class="round">
                <h1><%= @heading %></h1>
                <%= yield %>
            </section>
            <%= render 'layouts/footer' %>
        </dev>
    </body>
</html>